FROM ranjithrajd/epx-api:latest
ENV TEXT_SIMILARITY_SERVICE_URL=${TEXT_SIMILARITY_SERVICE_URL}
ENV TEXT_SIMILARITY_SERVICE_TOKEN=${TEXT_SIMILARITY_SERVICE_TOKEN}
ENV MONGO_USER=${MONGO_USER}
ENV MONGO_PASSWORD=${MONGO_PASSWORD}
ENV MONGO_CLUSTER=${MONGO_CLUSTER}
EXPOSE $PORT